# ApplicationControllerCpp
A simple, application controller and handler implemented in C++. Uses C++11 variadic templates.
Future versions of the controller will allow you to specify the return type.
